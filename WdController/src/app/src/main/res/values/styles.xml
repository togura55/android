<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/editTextFrame</item>
        <item name="colorControlActivated">@color/editTextFrame</item>
        <item name="android:textCursorDrawable">@color/editTextFrame</item>
        <item name="android:backgroundTint">@color/editTextFrame</item>
<!--                <item name="android:textColor">@color/orange500</item>
                <item name="android:textColorHint">@color/teal500</item>
                <item name="android:textColorHighlight">@color/purple500</item>-->
    </style>

</resources>
